# Configuration file for EventSetupTest_t

process TEST = {

	path p = { print, get }


	es_source = PoolDBESSource { VPSet toGet = {
                                   {string record = "EcalWeightRecAlgoWeightsRcd"
                                     string tag = "EcalWeightRecAlgoWeights_h4_sm5"
                                    } }
		    		    bool loadAll = true
                                    string connect = "sqlite_file:ecalwgt.db"
			            string timetype = "runnumber" 
				   }

	module print = AsciiOutputModule { }
	

	source = EmptySource {untracked int32 maxEvents = 10 untracked uint32 firstRun = 72999 untracked uint32 numberEventsInRun = 4}

	module get = EventSetupRecordDataGetter { VPSet toGet = {
	       {string record = "EcalWeightRecAlgoWeightsRcd"
	        vstring data = {"EcalWeightRecAlgoWeights"} } 
	       }
	       untracked bool verbose = true 
	}

}
